 {% extends 'base.html.twig' %} {# Template du formulaire de connection  #}

{% block title %}Connexion{% endblock %}

{% block body %}
   
    <div class="container">
        <div class="row">

            <div class="col-12 col-md-6 offset-md-3 my-5 px-5 border border-2 border-warning rounded background-bold px-5">
                <div class="mt-4 text-danger">
                    <strong>
		                {% if error %}
                            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                        {% endif %}
		            </strong>
	            </div>
                <div class="form-group mt-5 ">
                    <h1 class="text-center uppercaseb brown py-3 mb-5">Connexion</h1>
                    <form action="{{ path('login') }}" method="post"> 
                        <div class="mb-5">      
                            <label for="username" class="brown"><strong>Email :</strong></label>
                            <input type="text" id="username" name="_username"  class="form-control" value="{{ last_username }}"/>
                         </div>
                            <label for="password" class="brown"><strong>Mot de passe :</strong></label>
                             <input type="password" id="password" name="_password"  class="form-control"/>

                        {# If you want to control the URL the user is redirected to on success
                        <input type="hidden" name="_target_path" value="/account"/> #}
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                        <button type="submit" class="form-control btn btn-primary my-5">Login</button>
                    </form>
	            </div>
            </div>
        </div>
    </div>
{% endblock %}
